# Prefer `npm run deploy:init`, which generates a strong local password.
LOCAL_HOST=127.0.0.1
LOCAL_PORT=3000
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
SURVEY_BACKEND_URL=http://127.0.0.1:8000
DEPLOY_AUTH_ENABLED=true
DEPLOY_USERNAME=survey
DEPLOY_PASSWORD=replace-with-at-least-20-random-characters
